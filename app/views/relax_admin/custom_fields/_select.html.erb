<%= f.label a.keys.first, class: "form-control-label #{required?(f.object, a) ? 'required' : ''}" %>
<%= f.select(a.keys.first, a[a.keys.first][:choices], { include_blank: false, required: required?(f.object, a) }, class: 'selectize-single') %>
