<%= f.label a.keys.first, class: "form-control-label #{required?(f.object, a).present? ? 'required' : ''}" %>
<%= f.select(a.keys.first, a[a.keys.first][:choices], { include_blank: false }, class: 'selectize-single') %>
