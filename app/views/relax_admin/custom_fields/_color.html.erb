<%= f.label a.to_sym, class: "form-control-label #{required?(f.object, a).present? ? 'required' : ''}" %>
<%= f.text_field a, class: 'form-control colorpicker', required: required?(f.object, a).present? %>
