<%= f.label a, class: "form-control-label #{required?(f.object, a).present? ? 'required' : ''}" %>
<%= f.collection_select a.to_s + '_id', a.to_s.classify.constantize.all, :id, object_label(a), {prompt: "Sélectionner un(e) #{a.to_s.classify.constantize.model_name.human.downcase}"}, class: 'selectize-single', required: required?(f.object, a).present? %>
