<%= f.label a, class: "form-control-label #{required?(f.object, a).present? ? 'required' : ''}" %>
<%= f.collection_select a + '_id', a.classify.constantize.all, :id, object_label(a), {prompt: "Sélectionner un(e) #{a.classify.constantize.model_name.human.downcase}"}, class: 'selectize-single', required: required?(f.object, a).present? %>
