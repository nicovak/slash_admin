<%= f.label a, class: "form-control-label #{required?(f.object, a).present? ? 'required' : ''}" %>
<%= f.number_field a, class: 'form-control', step: '1', required: required?(f.object, a).present? %>
