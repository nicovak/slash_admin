<%= f.label a, class: "form-control-label required #{required?(f.object, a).present? ? 'required' : ''}" %>
<%= f.text_area a, class: 'form-control', rows: '5', required: required?(f.object, a).present? %>
